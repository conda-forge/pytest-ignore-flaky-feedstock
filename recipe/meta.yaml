{% set name = "pytest-ignore-flaky" %}
{% set version = "2.2.1" %}
{% set sha256 = "04416f6b2177ef1f2adcb9308a887bf1cf77e7062d71778e404f358d4bbef0c9" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name.replace('-', '_') }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . --no-deps -vv

requirements:
  host:
    - python
    - pip

  run:
    - python
    - pytest >=2.7

test:
  imports:
    - pytest_ignore_flaky

about:
  home: https://github.com/schettino72/pytest-ignore-flaky
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: 'ignore failures from flaky tests (pytest plugin)'
  dev_url: https://github.com/schettino72/pytest-ignore-flaky

extra:
  recipe-maintainers:
    - rmcgibbo
